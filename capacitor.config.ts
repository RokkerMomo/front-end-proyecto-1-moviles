import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'FrontEnd-Proyecto1-Moviles',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
